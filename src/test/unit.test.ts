import { describe, it } from "vitest";

describe("computeTacoAfterSetId", () => {
  it.todo("should return the set ID where taco appears after this set");
  it.todo("should handle edge case: taco before first set");
  it.todo("should handle edge case: taco after last set");
});

describe("isFestivalDay", () => {
  it.todo("should return true when current date matches FESTIVAL_DATE");
  it.todo("should return false when current date does not match");
});
